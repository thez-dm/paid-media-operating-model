# The system I'd build – paid media

A single-page answer to a job application question:

> Describe an AI-powered system, workflow, or automation you've personally built to support account targeting, campaign testing, or paid media optimisation. What problem did it solve, and what was the measurable outcome?

Rather than describe an automation built against bidding controls that no longer exist, this is the system I'd build now – four components in the order they have to be built, the approval gate that sits over them, and the outcome I'd sign up to be judged on at 90 days.

**Live:** https://thez-dm.github.io/paid-media-operating-model/

---

### Contents

| Section | What it covers |
| --- | --- |
| The problem | Why accounts have no memory and no standard of proof |
| The system | Context document → automation ladder → evidence rules → written trace |
| The gate | Every change arrives as a card a human signs |
| The split | What automation takes, what stays human |
| The outcome | 90-day plan and the six numbers to be measured on |
| Credits | The thinking that's borrowed, named |

Brand-agnostic on purpose: no product, budget, vertical or ad platform. This is the light version of a longer operating model covering channel strategy, the growth loop, cadence and measurement in full.

### Notes

- `index.html` is self-contained – no build step, no dependencies, no external requests.
- No figure on the page is a claim about past performance. The decision card is illustrative.

### Publishing

GitHub Pages, served from the `main` branch root. Settings → Pages → Source: *Deploy from a branch* → `main` / `/ (root)`.
